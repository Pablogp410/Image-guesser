@page
@model PictureGame.Pages.TestModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Home page";
}

<div>
    @if (Model.picture != null)
    {
            @Model.picture.Name

    }
    else
    {
        <p>There are no pictures</p>
    }
</div>
