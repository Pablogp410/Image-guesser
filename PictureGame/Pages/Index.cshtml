@page
@model PictureGame.Pages.IndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome to the Picture Game!</h1>
    <form method="post">
        <div>
            Username: <input type="text" asp-for="Username" name="Username" />
        </div>
        <div>
            Password: <input type="password" asp-for="Password" name="Password" />
        </div>
        <button type="submit">Login</button>
        @if(Model.Errors.Length > 0)
    {
        <div class="alert alert-danger">
            <ul>
                @foreach (var erro in Model.Errors)
                {
                    <li>@erro</li>
                }
            </ul>
        </div>
    } 
    </form>
    <h2 class="display-4">Dont have user?</h2>
    <button> 
        <a href="Register">Register</a>
    </button>
     <button> 
        <a href="Menu">Access as a guest</a>
    </button>
    
</div>
